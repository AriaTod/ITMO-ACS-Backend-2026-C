$kind: http-request
url: "{{baseUrl}}/recipes/{{recipeId}}/favorite"
method: POST
auth:
  type: bearer
  credentials:
    token: "{{userToken}}"
scripts:
  - type: afterResponse
    code: pm.test('Статус 204', () => pm.response.to.have.status(204));
    language: text/javascript
order: 13000
